<%= form_for(@threesixty) do |f| %>
  <% if @threesixty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@threesixty.errors.count, "error") %> prohibited this threesixty from being saved:</h2>

      <ul>
      <% @threesixty.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
