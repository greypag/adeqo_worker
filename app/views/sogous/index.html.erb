sogous/index

<%
# 
# concat "<hr />".html_safe  
# concat @acc_file_id
# 
# concat "<hr />".html_safe
#   
# @db[:campaign_sogou].find.sort('id' => Mongo::Index::DESCENDING).each do |network|
	# concat network
	# concat "<br />".html_safe 
# end
#   
# concat "<hr />".html_safe
# 
# 
# @db[:adgroup_sogou].find().sort('id' => Mongo::Index::DESCENDING).each_with_index do |adgroup, index|
	# concat adgroup
	# concat "<br />".html_safe
# end
#   
# concat "<hr />".html_safe
# 
# @db[:ad_sogou].find().sort('id' => Mongo::Index::DESCENDING).each_with_index do |ad, index|
	# concat ad
	# concat "<br />".html_safe
# end
#   
# concat "<hr />".html_safe
# 
# 
# concat "<hr />".html_safe
# 
# @db[:keyword_sogou].find().sort('id' => Mongo::Index::DESCENDING).each_with_index do |ad, index|
	# concat ad
	# concat "<br />".html_safe
# end
#   
# concat "<hr />".html_safe

%>
